import React from 'react';
import NotFound from "../../img/NotFound/NotFound";

const NotFoundPage = () => {
    return (
        <div>
            <NotFound/>
        </div>
    );
};

export default NotFoundPage;